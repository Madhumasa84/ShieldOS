ShieldOS exists with basic stats.
Add full analytics and exportable reports.

════════════════════════════════════════════
ADD — ANALYTICS DASHBOARD + REPORTS
════════════════════════════════════════════

1. ANALYTICS PAGE (/analytics):

TIME RANGE SELECTOR:
- Today / Last 7 days / Last 30 days / Custom range

CHARTS (use recharts, already installed):
a) Blocked Requests Over Time
   - Line chart, hourly granularity
   - Overlay: total queries vs blocked

b) Block Rate Trend
   - Area chart showing % blocked over time
   - Average line

c) Top 20 Blocked Domains
   - Horizontal bar chart
   - Click domain → see full history

d) Category Breakdown
   - Donut chart: ads/tracking/malware/social
   - Click slice → filter table below

e) Per-Device Comparison
   - Grouped bar chart
   - Each device as a series
   - Compare blocking across devices

f) Threat Timeline
   - Scatter plot: time vs severity
   - Dot size = frequency
   - Color = category

2. ANALYTICS API ENDPOINTS:
GET /api/v1/analytics/overview?range=7d
GET /api/v1/analytics/blocked-over-time?range=30d&granularity=hour
GET /api/v1/analytics/top-domains?range=7d&limit=20
GET /api/v1/analytics/by-category?range=7d
GET /api/v1/analytics/by-device?range=7d
GET /api/v1/analytics/threats?range=30d

3. EXPORTABLE REPORTS:
GET /api/v1/reports/generate?range=30d&format=pdf

PDF Report includes:
- ShieldOS header with generation date
- Executive summary (key numbers)
- All charts rendered as images
- Top blocked domains table
- Device activity table
- Threat events summary
- Footer: "Generated by ShieldOS"

GET /api/v1/reports/generate?range=30d&format=csv
CSV with all raw data

GET /api/v1/reports/generate?range=30d&format=json
Full JSON dump

4. REPORT SCHEDULER:
- Settings → "Scheduled Reports" section
- Configure: weekly / monthly report
- Delivery: download link or webhook
- Auto-generate and store last 12 reports
- GET /api/v1/reports/history → list past reports
- GET /api/v1/reports/{id}/download → download report

Add all to OpenAPI spec + React Query hooks.